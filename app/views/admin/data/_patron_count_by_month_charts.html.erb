<% context.instance_eval do
	panel 'Patron Count By Year For Each Branch' do
		div id: 'ajax_data', 'data-ajax' => opts.to_json
		div id: 'loading' do
			img id: "ajaxSpinnerImage", src: image_url("ajax-loader.gif"), title: "working", class: 'centered'
		end
		div id: "count_by_month"
	end

end %>